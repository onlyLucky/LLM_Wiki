---
date: 2026-09-09
tags: [daily, feishu, wiki, git, push, games101, hugo, blog, deploy, model-config]
sessions: 2
messages: 101
platform: [feishu]
---

# 2026-09-09 每日会话汇总

> 本日 2 个飞书用户会话（18:38–19:47，跨 default/assistant 两个 profile），三件事收尾：wiki 分类提交推送、双机器人模型切换、博客打包部署。

## 📌 今日主题

1. **Wiki 分类提交并推送远端** — 闭环 09-08 汇总的待跟进（push 后新补的 GAMES101 配图 + 09-08 每日汇总），两个 commit 上线
2. **双机器人模型切换** — 两个 profile 的模型统一改为 `glm-5.3-flash` 并重启 gateway
3. **webHugo 博客部署** — 本地累积 4 个 commit 的更新打包上线腾讯云
4. **模型配置确认** — 重启后模型持久性确认（顶层 `model.default` 为生效配置）

## ✅ 完成的任务

### 1. Wiki 分类提交 + push（default，@session:default/20260909_183803_fb6a527e）
- `git status` 盘点：modified（index.md、log.md、06-几何.md、chap6_22_1.gif、chap6_25_1.png）+ untracked（daily/2026-09-08.md + 22 张 GAMES101 配图）
- 按 commit-conventions 分类两个 commit 并推送：
  - `f702674` `reading:` — GAMES101 第6讲几何配图补齐，23 张 chap6 配图替换截图占位符 + 知识点通俗化（24 files）
  - `6d44526` `daily:` — 09-08 每日汇总 + index/log 联动（3 files）
- push 成功：`d572cdf..6d44526 main -> main`，**闭环 09-08 汇总的待跟进事项**

### 2. 双机器人模型切换为 glm-5.3-flash（default，同会话）
- 两个 profile 的 `model.default` 均改为 `glm-5.3-flash`（provider: douyincodingplan / 火山方舟）
- assistant gateway 重启（PID 74773）；default gateway `--replace` 重启（会话短暂中断后自动恢复）
- 验证：两个 profile 均加载 glm-5.3-flash ✓

### 3. webHugo 博客打包部署（assistant，@session:assistant/20260909_193626_4439011f）
- `hugo --minify`：230 页 / 326 静态文件，814ms
- rsync 增量同步 28MB → `ubuntu@124.222.199.171:/data/www/HugoBlog/dist/`
- 线上验证全部 HTTP 200：首页、GAMES101 几何/着色、html-in-canvas-exploration、threejs-noise
- 上线内容 = 最近 4 个 commit（GAMES101 几何中英文、HTML-in-Canvas 探索系列、GAMES101 第5课着色、Three.js 第12课噪声）

### 4. 模型配置答疑（assistant，同会话）
- 确认 `config.yaml` 顶层 `model.default` 才是实际生效配置；`custom_providers` 内的 `model` 字段只是 provider 兜底默认值
- 结论：重启后依旧使用 glm-5.3-flash ✓

## 🔑 关键发现

- **模型配置层级关系**：顶层 `model.default: glm-5.3-flash` 是实际生效值，custom_providers 里的 `model: deepseek-v4-flash-ga-260731` 仅在顶层未指定时兜底——记忆中「两 profile 模型 = deepseek-v4-flash-ga-260731」的信息已过时，现为 glm-5.3-flash
- **launchd exit 5 已知问题持续**：这台 macOS 上 launchd 无法托管 gateway（Bootstrap failed: 5），两个 gateway 均以 detached 后台进程运行——无开机自启、崩溃不自动重启
- **两机器人并行使用**：用户同时与两个机器人对话（一个管 wiki/配置、一个管博客部署），会话时间线交错

## 📋 待跟进事项

- 09-09 的汇总文件本身（本文件 + index.md + log.md 更新）待用户确认后按 `daily:` 提交推送
- `.obsidian/workspace.json` 是否 `git rm --cached` 彻底解除跟踪——多次提议，仍待用户确认
- gateway 以后台进程运行，重启电脑后需手动 `hermes gateway start` + `hermes -p assistant gateway start`

## 📊 平台统计

| 平台 | 会话数 | 消息 | 用户消息 |
|------|--------|------|----------|
| 飞书（default） | 1 | 77 | 5 |
| 飞书（assistant） | 1 | 24 | 3 |
| **合计** | **2** | **101** | **8** |
