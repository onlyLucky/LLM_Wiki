## 章节内容

光线追踪 (Ray Tracing) 是图形学中一种通过模拟光线传播路径来生成图像的渲染技术。与光栅化相比，光线追踪能够准确地模拟全局光照效果（如反射、折射、软阴影、间接光照等），是离线渲染（电影、动画）的核心技术。

> 本章对应课程第 13-18 讲
> - 第 13 讲 [课件 PDF](https://sites.cs.ucsb.edu/~lingqi/teaching/resources/GAMES101_Lecture_13.pdf)（Ray Tracing 1: Whitted-Style Ray Tracing）
> - 第 14 讲 [课件 PDF](https://sites.cs.ucsb.edu/~lingqi/teaching/resources/GAMES101_Lecture_14.pdf)（Ray Tracing 2: Acceleration & Radiometry）
> - 第 15 讲 [课件 PDF](https://sites.cs.ucsb.edu/~lingqi/teaching/resources/GAMES101_Lecture_15.pdf)（Light Transport & Global Illumination）
> - 第 16 讲 [课件 PDF](https://sites.cs.ucsb.edu/~lingqi/teaching/resources/GAMES101_Lecture_16.pdf)（Ray Tracing 4: Monte Carlo, Path Tracing）
> - 第 17 讲 [课件 PDF](https://sites.cs.ucsb.edu/~lingqi/teaching/resources/GAMES101_Lecture_17.pdf)（Materials and Appearances）
> - 第 18 讲 [课件 PDF](https://sites.cs.ucsb.edu/~lingqi/teaching/resources/GAMES101_Lecture_18.pdf)（Advanced Topics in Rendering）
> - [B站视频](https://www.bilibili.com/video/BV1X7411F744)

---

### 1、为什么需要光线追踪

#### 光栅化的局限性

光栅化是一种快速的渲染技术，但在处理**全局效果**方面存在明显不足：

| 效果 | 光栅化能否处理 | 说明 |
|:---:|:---:|:---|
| **软阴影** | 困难 | 需要计算半影区域 |
| **多次反弹光照** | 不能 | 光栅化主要处理直接光照 |
| **镜面反射** | 不能 | 需要追踪反射光线 |
| **折射** | 不能 | 需要追踪折射光线 |
| **环境光遮蔽 (AO)** | 困难 | 需要几何体间的遮挡关系 |
| **全局光照 (GI)** | 不能 | 需要模拟光的多次弹射 |

<!-- 截图：光栅化无法处理的效果示例（软阴影、反射、AO、GI）（L13 第4页）→ 保存为 ./images/chap7_01.png -->

#### 光线追踪的优缺点

| 特性 | 光栅化 | 光线追踪 |
|:---:|:---|:---|
| **速度** | 快（实时） | 慢（离线） |
| **质量** | 相对较低 | 高（物理准确） |
| **全局效果** | 难以处理 | 自然支持 |
| **应用** | 游戏、实时渲染 | 电影、动画（约 10K CPU 核心时/帧） |

光栅化的核心问题是：当光线弹射超过一次时，光栅化就难以正确处理。光线追踪通过递归地追踪光线路径来解决这个问题。

---

### 2、光线的基本概念

#### 光线的三个假设

1. 光沿直线传播（虽然物理上不完全正确）
2. 光线之间不会相互碰撞（虽然物理上不完全正确）
3. 光线从光源传播到眼睛（但物理规律在路径反转下不变 —— **可逆性**）

<!-- 截图：光线传播的三个假设（L13 第8页）→ 保存为 ./images/chap7_02.png -->

#### 射线理论 (Emission Theory of Vision)（课件外补充）

历史上曾认为眼睛发射"感觉光线"到世界中。实际上，光从光源出发，经过多次反射/折射最终到达眼睛。由于光路的可逆性，我们可以从眼睛（相机）出发，反向追踪光线到光源。

---

### 3、光线投射 (Ray Casting)

光线投射由 Appel 于 1968 年提出，是光线追踪的最基础形式：

1. 对每个像素，从相机发射一条光线（**主光线 / 眼睛射线**）
2. 找到光线与场景中物体的最近交点
3. 从交点向每个光源发射**阴影光线**，检查是否被遮挡
4. 根据交点处的着色计算像素颜色（仅局部光照，不包含反射/折射）

<!-- 截图：Ray Casting 针孔相机模型（L13 第11页）→ 保存为 ./images/chap7_03.png -->

#### 针孔相机模型中的光线投射

```
相机(eye) → 像素 → 场景交点 → 光源
```

- 从相机出发，穿过每个像素中心发射一条光线
- 找到光线与场景的最近交点
- 从该交点向光源发射阴影光线，判断是否被遮挡
- 计算局部着色（如 Blinn-Phong 模型）

**局限**：只计算了直接光照，没有反射和折射。

---

### 4、Whitted-Style 光线追踪

#### 核心思想

Whitted 于 1980 年提出递归光线追踪（"An improved illumination model for shaded display"），在 Ray Casting 基础上增加了**递归追踪反射和折射光线**的能力。

**渲染耗时演进**（L13 第13页）：Whitted 1979 年的经典图 "Spheres and Checkerboard"，在 VAX 11/780 (1979) 上渲染约需 74 分钟，PC (2006) 上约 6 秒，GPU (2012) 上约 1/30 秒。

<!-- 截图：Whitted 1979 年渲染的 Spheres and Checkerboard（L13 第13页）→ 保存为 ./images/chap7_05.png -->

<!-- 截图：Whitted-Style 光线追踪示意图（L13 第18页）→ 保存为 ./images/chap7_04.png -->

#### 算法流程

1. 从相机发射主光线穿过像素
2. 找到最近交点
3. 在交点处计算直接光照（向光源发射阴影光线）
4. 如果表面是**镜面反射**材质，递归地发射**反射光线**
5. 如果表面是**透明/折射**材质，递归地发射**折射光线**
6. 最终颜色 = 直接光照 + 反射贡献 + 折射贡献

#### 光线类型术语（L13 第14-18页）

| 术语 | 含义 |
|:---|:---|
| **主光线 (primary ray)** | 从相机出发、穿过像素进入场景的光线 |
| **次级光线 (secondary rays)** | 在交点处递归产生的反射光线（镜面反射）与折射光线（镜面透射） |
| **阴影光线 (shadow rays)** | 从着色点射向光源、用于判断遮挡的光线 |

<!-- 截图：递归光线追踪分步示意：反射光线、折射光线、阴影光线（L13 第14-18页）→ 保存为 ./images/chap7_06.png -->

#### 伪代码（课件外补充）

```
trace(ray, depth):
    if depth > MAX_DEPTH:
        return BLACK

    hit = find_nearest_intersection(ray, scene)
    if not hit:
        return BACKGROUND_COLOR

    color = shade(hit)  // 直接光照

    if hit.material is reflective:
        reflect_ray = reflect(ray, hit.normal)
        color += kr * trace(reflect_ray, depth + 1)

    if hit.material is transparent:
        refract_ray = refract(ray, hit.normal, ior)
        color += kt * trace(refract_ray, depth + 1)

    return color
```

#### Whitted-Style 的问题

- **镜面材质**：总是执行完美的镜面反射/折射，不适用于光泽 (glossy) 材质
- **漫反射表面**：光线在漫反射表面停止弹射，丢失了漫反射间的间接光照
- **不是物理正确的**：无法产生色彩渗透 (color bleeding) 等全局光照效果

这些问题将在后续的路径追踪 (Path Tracing) 中得到解决。

---

### 5、光线-表面交点计算

#### 光线方程

光线由**起点 (origin)** $\vec{o}$ 和**方向向量 (direction)** $\vec{d}$（归一化）定义：

$$
\vec{r}(t) = \vec{o} + t\vec{d}, \quad 0 \leq t < \infty
$$

其中 $t$ 是沿光线方向的参数（"时间"）。

<!-- 截图：光线方程示意图（L13 第21页）→ 保存为 ./images/chap7_07.png -->

#### 光线与球面求交

球面方程（中心 $\vec{c}$，半径 $R$）：

$$
(\vec{p} - \vec{c})^2 - R^2 = 0
$$

将光线方程代入球面方程：

$$
(\vec{o} + t\vec{d} - \vec{c})^2 - R^2 = 0
$$

展开得到一元二次方程 $at^2 + bt + c = 0$，其中：

$$
a = \vec{d} \cdot \vec{d}
$$

$$
b = 2(\vec{o} - \vec{c}) \cdot \vec{d}
$$

$$
c = (\vec{o} - \vec{c}) \cdot (\vec{o} - \vec{c}) - R^2
$$

求解：

$$
t = \frac{-b \pm \sqrt{b^2 - 4ac}}{2a}
$$

- 取最小的正实数解 $t$ 作为交点
- 若判别式 $b^2 - 4ac < 0$，光线与球面无交点

#### 光线与隐式曲面求交

一般隐式曲面：$\vec{p} : f(\vec{p}) = 0$

将光线方程代入：$f(\vec{o} + t\vec{d}) = 0$

求解实数、正的根即可。

#### 光线与平面求交

平面由法向量 $\vec{N}$ 和平面上一点 $\vec{p}'$ 定义：

$$
(\vec{p} - \vec{p}') \cdot \vec{N} = 0
$$

将光线方程代入：

$$
(\vec{o} + t\vec{d} - \vec{p}') \cdot \vec{N} = 0
$$

解得：

$$
t = \frac{(\vec{p}' - \vec{o}) \cdot \vec{N}}{\vec{d} \cdot \vec{N}}
$$

检查 $0 \leq t < \infty$。若 $\vec{d} \cdot \vec{N} = 0$，光线与平面平行，无交点。

#### 光线与三角形求交

**为什么需要**：
- 渲染：可见性、阴影、光照
- 几何：内外测试

**基本思路**：三角形在平面内，因此分两步：
1. 光线与三角形所在平面求交
2. 判断交点是否在三角形内部

#### Moller-Trumbore 算法

一种更快的直接方法，能够同时给出交点参数 $t$ 和重心坐标 $(b_1, b_2)$。

设三角形顶点为 $\vec{P}_0, \vec{P}_1, \vec{P}_2$，光线起点 $\vec{O}$，方向 $\vec{D}$：

$$
\vec{O} + t\vec{D} = (1 - b_1 - b_2)\vec{P}_0 + b_1\vec{P}_1 + b_2\vec{P}_2
$$

定义辅助变量：

$$
\vec{E}_1 = \vec{P}_1 - \vec{P}_0, \quad \vec{E}_2 = \vec{P}_2 - \vec{P}_0
$$

$$
\vec{S} = \vec{O} - \vec{P}_0, \quad \vec{S}_1 = \vec{D} \times \vec{E}_2, \quad \vec{S}_2 = \vec{S} \times \vec{E}_1
$$

解为：

$$
\begin{pmatrix} t \\ b_1 \\ b_2 \end{pmatrix} = \frac{1}{\vec{S}_1 \cdot \vec{E}_1} \begin{pmatrix} \vec{S}_2 \cdot \vec{E}_2 \\ \vec{S}_1 \cdot \vec{S} \\ \vec{S}_2 \cdot \vec{D} \end{pmatrix}
$$

**判断条件**：
- $t \geq 0$：交点在光线上
- $b_1 \geq 0, \; b_2 \geq 0, \; 1 - b_1 - b_2 \geq 0$：交点在三角形内

计算代价：1 次除法、27 次乘法、17 次加法（课件外补充，来自 Möller-Trumbore 论文）。

---

### 6、光线追踪加速结构

#### 性能问题

朴素方法：对每条光线测试与每个三角形的交点，找最近的交点。

| 场景 | 三角形数量 |
|:---|:---|
| San Miguel | 10.7M |
| Plant Ecosystem | 20M |

朴素算法的复杂度约为：`#像素 x #三角形 x #弹射次数`，极其缓慢。

#### 包围盒 (Bounding Volume)

**核心思想**：用简单的几何体（如盒子）包围复杂物体。如果光线不与包围盒相交，则一定不与内部物体相交，可以直接跳过。

<!-- 截图：包围盒加速原理（L13 第35页）→ 保存为 ./images/chap7_08.png -->

#### 轴对齐包围盒 (AABB)

**轴对齐包围盒 (Axis-Aligned Bounding Box, AABB)**：盒子的每个面都与 x、y、z 轴平行。

**为什么用轴对齐**：计算光线与轴对齐面的交点比一般平面快得多。

| 类型 | 计算公式 | 运算量 |
|:---|:---|:---|
| **一般平面** | $t = \frac{(\vec{p}' - \vec{o}) \cdot \vec{N}}{\vec{d} \cdot \vec{N}}$ | 3 减 + 6 乘 + 1 除 |
| **轴对齐面** | $t = \frac{p_x' - o_x}{d_x}$ | 1 减 + 1 除 |

#### 光线与 AABB 求交 (Slab Method)

**理解**：3D 盒子 = 3 对平行平板 (slab) 的交集。

**2D 示例（3D 同理）**：

对每对 slab（x、y、z 方向各一对），计算光线进入和离开的 $t$ 值：

- 对每对 slab 计算 $t_{min}$ 和 $t_{max}$
- 3D 盒子的进入时间：$t_{enter} = \max\{t_{min}\}$（取所有方向 $t_{min}$ 的最大值）
- 3D 盒子的离开时间：$t_{exit} = \min\{t_{max}\}$（取所有方向 $t_{max}$ 的最小值）

<!-- 截图：光线与 AABB 求交的 Slab Method（L13 第37页）→ 保存为 ./images/chap7_09.png -->

**关键判断**：

- 光线只有进入**所有** slab 对后，才算进入盒子
- 光线只要离开**任意**一对 slab，就离开了盒子
- 若 $t_{enter} < t_{exit}$，光线在盒子内停留了一段时间，即有交点

**额外检查**（光线不是直线，需检查 $t$ 的正负）：

| 情况 | 条件 | 结论 |
|:---|:---|:---|
| $t_{exit} < 0$ | 盒子在光线后方 | 无交点 |
| $t_{exit} \geq 0$ 且 $t_{enter} < 0$ | 光线起点在盒子内 | 有交点 |
| 一般情况 | $t_{enter} < t_{exit}$ 且 $t_{exit} \geq 0$ | 有交点 |

**总结**：光线与 AABB 相交当且仅当：

$$
t_{enter} < t_{exit} \quad \text{且} \quad t_{exit} \geq 0
$$

---

### 7、加速结构：空间划分 vs 物体划分

#### 均匀空间划分 (Uniform Grids)

**构建步骤**：
1. 计算场景包围盒
2. 将包围盒划分为均匀网格
3. 将每个物体存入其覆盖的网格单元中

**光线遍历**：
- 按光线穿过网格的顺序逐个检查网格（课件外补充：即 3D DDA 算法）
- 对每个网格单元内的物体进行相交测试

<!-- 截图：均匀网格加速（L14 第9页）→ 保存为 ./images/chap7_10.png -->

**网格分辨率的经验法则**：

**网格分辨率的两个极端**（L14 第10-11页）：
- 单元格太少（极端：整个场景一个格子）→ 每条光线仍需测试所有物体，**无加速效果**
- 单元格太多 → 时间浪费在**遍历网格本身**上

因此采用启发式：

$$
\#\text{cells} = C \times \#\text{objs}, \quad C \approx 27 \text{（3D）}
$$

**适用场景**与**不适用场景**：

| 场景 | 效果 | 说明 |
|:---|:---|:---|
| 物体大小和空间分布均匀 | 好 | 网格能有效跳过空白区域 |
| "体育场里的茶壶"问题 | 差 | 空间中物体分布极不均匀，大量网格为空 |

#### 空间划分 (Spatial Partitioning)

将空间划分为不重叠的区域。常见方法：

| 方法 | 说明 |
|:---|:---|
| **KD-Tree** | 交替沿 x/y/z 轴用平面递归分割空间 |
| **Oct-Tree** | 递归地将空间分成 8 个子立方体（3D）/ 4 个子方形（2D） |
| **BSP-Tree** | 用任意平面递归分割空间（不限于轴对齐） |

<!-- 截图：空间划分示例：Oct-Tree / KD-Tree / BSP-Tree（L14 第16页）→ 保存为 ./images/chap7_11.png -->

##### KD-Tree

**数据结构**：
- **内部节点**存储：分割轴（x/y/z）、分割位置、子节点指针（不存储物体）
- **叶节点**存储：物体列表

**遍历**：
1. 从根节点开始，判断光线是否与节点包围盒相交
2. 若是内部节点，根据分割平面决定遍历顺序（先近后远）
3. 若是叶节点，测试与所有物体的交点
4. 返回最近交点

**KD-Tree 的问题**：
- 物体可能跨越多个区域，被存储在多个叶节点中
- 三角形与包围盒的求交判断较复杂

#### 物体划分：BVH (Bounding Volume Hierarchy)

**核心思想**：将物体集合递归地分成两组，每组计算包围盒。与空间划分不同，BVH 划分的是物体而不是空间。

<!-- 截图：BVH 构建过程（L14 第33页）→ 保存为 ./images/chap7_12.png -->

##### BVH 构建算法

1. 计算当前物体集合的包围盒
2. 递归地将物体集合分成两个子集
3. 对每个子集重新计算包围盒
4. 满足终止条件时停止
5. 在叶节点中存储物体列表

**如何划分子集**（启发式规则）：
- **启发式 1**：始终选择节点中最长的轴进行分割
- **启发式 2**：在物体中位数位置进行分割（保证两棵子树平衡）

**终止条件**：
- 当节点包含的物体数量足够少时（如 5 个）停止

##### BVH 数据结构

- **内部节点**存储：包围盒、子节点指针
- **叶节点**存储：包围盒、物体列表
- 每个节点代表场景中物体的一个子集

##### BVH 遍历算法

```
Intersect(Ray ray, BVH node) {
    if (ray misses node.bbox) return;

    if (node is leaf node)
        test intersection with all objs;
        return closest intersection;

    hit1 = Intersect(ray, node.child1);
    hit2 = Intersect(ray, node.child2);
    return the closer of hit1, hit2;
}
```

#### 空间划分 vs 物体划分对比

| 特性 | 空间划分 (KD-Tree) | 物体划分 (BVH) |
|:---|:---|:---|
| **划分对象** | 空间 | 物体集合 |
| **区域重叠** | 不重叠 | 包围盒可能重叠 |
| **物体归属** | 一个物体可能在多个区域 | 每个物体只属于一个子集 |
| **适用性** | 静态场景 | 动态场景（可重新构建局部） |
| **实际使用** | 较少 | 最常用 |

BVH 是目前光线追踪中最常用的加速结构。

注：表中"适用性""实际使用"两行为课件外补充（业界常识），课件仅对比了划分对象、区域重叠与物体归属。

---

### 8、辐射度量学 (Radiometry)

#### 动机

在之前的 Blinn-Phong 模型中，光照强度 $I$ 是一个数字（如 10），但"10 什么？"没有明确的物理含义。辐射度量学提供了**物理正确**的光照度量体系。

辐射度量学是测量光照的系统和方法，能够准确度量光的空间属性。

> 辐射度量学的内容从第 14 讲第 40 页开始（L14 后半），延续到第 15 讲前半。

#### 基本概念

| 量 | 符号 | 定义 | 单位 |
|:---|:---|:---|:---|
| **辐射能 (Radiant Energy)** | $Q$ | 电磁辐射的能量 | 焦耳 (J = Joule) |
| **辐射通量 (Radiant Flux / Power)** | $\Phi$ | 单位时间的辐射能 $\Phi = \frac{dQ}{dt}$ | 瓦特 (W = Watt) 或流明 (lm = lumen) |
| **辐射强度 (Radiant Intensity)** | $I(\omega)$ | 单位立体角的功率 $I = \frac{d\Phi}{d\omega}$ | W/sr 或 cd (candela) |
| **辐照度 (Irradiance)** | $E(x)$ | 单位面积的功率 $E = \frac{d\Phi}{dA}$ | $W/m^2$ 或 lux |
| **辐射率 (Radiance)** | $L(p, \omega)$ | 单位立体角、单位投影面积的功率 | $W/(sr \cdot m^2)$ 或 nit |

<!-- 截图：辐射度量学各概念的关系图（L14 第45页）→ 保存为 ./images/chap7_13.png -->

#### 立体角 (Solid Angle)

**角度 (Angle)**：弧长与半径之比，$\theta = \frac{l}{r}$，整个圆 $2\pi$ 弧度。

**立体角 (Solid Angle)**：球面上的面积与半径平方之比：

$$
\Omega = \frac{A}{r^2}
$$

整个球面 $4\pi$ 球面度 (steradian, sr)。

**微分立体角 (Differential Solid Angle)**：

$$
dA = (r \, d\theta)(r \sin\theta \, d\phi) = r^2 \sin\theta \, d\theta \, d\phi
$$

$$
d\omega = \frac{dA}{r^2} = \sin\theta \, d\theta \, d\phi
$$

<!-- 截图：微分立体角的推导（L14 第49页）→ 保存为 ./images/chap7_14.png -->

**验证：整个球面的立体角**（L14 第50页）：

$$
\Omega = \int_{S^2} d\omega = \int_0^{\pi} \int_0^{2\pi} \sin\theta \, d\phi \, d\theta = 2\pi \left[ -\cos\theta \right]_0^{\pi} = 4\pi
$$

#### 辐射强度 (Radiant Intensity)

$$
I(\omega) = \frac{d\Phi}{d\omega}
$$

**各向同性点光源**：

$$
\Phi = \int_{S^2} I \, d\omega = 4\pi I \implies I = \frac{\Phi}{4\pi}
$$

**示例：815 流明的 LED 灯（各向同性），辐射强度 = $815 / (4\pi) \approx 65$ 坎德拉**（L14 第53页）。

**补充**（L14 第47页）：坎德拉 (candela) 是国际单位制 (SI) 的**七个基本单位之一**。

#### 辐照度 (Irradiance)

$$
E(x) = \frac{d\Phi(x)}{dA}
$$

**朗伯余弦定律 (Lambert's Cosine Law)**：辐照度与光线方向和表面法线夹角的余弦成正比：

$$
E = \frac{\Phi}{A} \cos\theta
$$

<!-- 截图：朗伯余弦定律：立方体顶面旋转 60° 后接收功率减半（L15 第10页）→ 保存为 ./images/chap7_15.png -->

**立方体示例**（L15 第10页）：立方体顶面接收功率 $E = \Phi/A$；将立方体旋转 60° 后，顶面接收的功率减半，$E = \Phi/2A$。一般地，单位面积接收的功率正比于 $\cos\theta = l \cdot n$。

**应用**：四季更替 —— 地球自转轴倾斜约 23.5 度，导致太阳光入射角变化，辐照度随余弦变化。

**辐照度衰减**：点光源在距离 $r$ 处的辐照度与 $r^2$ 成反比：

$$
E = \frac{\Phi}{4\pi r^2}
$$

#### 辐射率 (Radiance)

辐射率是描述环境中光分布的**基本场量**，是与一条光线关联的量。渲染的本质就是计算辐射率。

$$
L(p, \omega) = \frac{d^2\Phi(p, \omega)}{d\omega \, dA \cos\theta}
$$

其中 $\cos\theta$ 用于考虑投影面积。

**辐射率与其他量的关系**：

| 关系 | 表达 |
|:---|:---|
| 辐射率 = 辐照度 / 立体角 | $L(p, \omega) = \frac{dE(p)}{d\omega \cos\theta}$ |
| 辐射率 = 辐射强度 / 投影面积 | $L(p, \omega) = \frac{dI(p, \omega)}{dA \cos\theta}$ |

**入射辐射率 (Incident Radiance)**：到达表面的单位立体角辐照度。

**出射辐射率 (Exiting Radiance)**：离开表面的单位投影面积辐射强度。

#### 辐照度与辐射率的关系

辐照度 = 所有方向的入射辐射率的积分：

$$
E(p) = \int_{H^2} L_i(p, \omega) \cos\theta \, d\omega
$$

其中 $H^2$ 是单位半球，$\cos\theta$ 是入射方向与法线的夹角。

---

### 9、双向反射分布函数 (BRDF)

#### 反射的物理过程

光线从方向 $\omega_i$ 入射到表面点 $p$：
1. 入射辐射率 $L(\omega_i)$ 在 $dA$ 上产生微分辐照度 $dE(\omega_i) = L(\omega_i) \cos\theta_i \, d\omega_i$
2. 该辐照度被表面反射到各个出射方向 $\omega_r$
3. 产生微分出射辐射率 $dL_r(\omega_r)$

#### BRDF 定义

BRDF 表示从每个入射方向反射到每个出射方向的光的比例：

$$
f_r(\omega_i \to \omega_r) = \frac{dL_r(\omega_r)}{dE_i(\omega_i)} = \frac{dL_r(\omega_r)}{L_i(\omega_i) \cos\theta_i \, d\omega_i} \quad \left[\frac{1}{sr}\right]
$$

<!-- 截图：BRDF 反射示意（L15 第21页）→ 保存为 ./images/chap7_16.png -->

**物理意义**：BRDF 完全描述了材质的反射特性 —— 材质在图形学中就是 BRDF。

---

### 10、反射方程 (Reflection Equation)

#### 推导

某点 $p$ 沿方向 $\omega_r$ 的出射辐射率 = 所有入射方向贡献的积分：

$$
L_r(p, \omega_r) = \int_{H^2} f_r(p, \omega_i \to \omega_r) \, L_i(p, \omega_i) \cos\theta_i \, d\omega_i
$$

其中：
- $f_r$：BRDF（反射多少）
- $L_i$：入射辐射率（来自光源或其他物体反射的光）
- $\cos\theta_i$：入射角的余弦（朗伯余弦定律）
- $H^2$：单位半球（所有入射方向）

#### 递归性

反射方程存在递归问题：出射辐射率取决于入射辐射率，而入射辐射率又取决于其他点的反射辐射率。这个递归关系最终由**渲染方程**统一描述。

---

### 11、渲染方程 (Rendering Equation)

#### 推导

在反射方程的基础上，添加**自发光项** (Emission term) 使其成为一般形式：

$$
L_o(p, \omega_o) = L_e(p, \omega_o) + \int_{\Omega^+} L_i(p, \omega_i) \, f_r(p, \omega_i, \omega_o) \, (n \cdot \omega_i) \, d\omega_i
$$

其中：
- $L_o(p, \omega_o)$：点 $p$ 沿 $\omega_o$ 方向的出射辐射率（未知，待求）
- $L_e(p, \omega_o)$：自发光辐射率（已知，光源直接发出的光）
- $L_i(p, \omega_i)$：来自 $\omega_i$ 方向的入射辐射率（未知，来自其他点反射的光）
- $f_r(p, \omega_i, \omega_o)$：BRDF（已知，由材质决定）
- $(n \cdot \omega_i) = \cos\theta_i$：入射角的余弦（已知）
- $\Omega^+$：上半球（所有方向指向外部）

**注意**：假设所有方向都指向外部。

渲染方程由 Kajiya 于 **1986 年**提出（L15 第31页）。

<!-- 截图：渲染方程各项含义（L15 第30页）→ 保存为 ./images/chap7_17.png -->

#### 物理意义

| 项 | 含义 | 已知/未知 |
|:---|:---|:---:|
| $L_o(p, \omega_o)$ | 出射辐射率（输出图像） | 未知 |
| $L_e(p, \omega_o)$ | 自发光 | 已知 |
| $L_i(p, \omega_i)$ | 入射辐射率（来自光源或其他物体） | 未知 |
| $f_r(p, \omega_i, \omega_o)$ | BRDF | 已知 |
| $n \cdot \omega_i$ | 入射角余弦 | 已知 |

#### 渲染方程的数学理解

渲染方程是一个 **Fredholm 第二类积分方程**，规范形式为：

$$
I(u) = \epsilon(u) + \int I(v) K(u, v) \, dv
$$

其中 $K(u,v)$ 是方程的核 (kernel)。

写成线性算子形式：

$$
L = E + KL
$$

其中 $K$ 是光照传输算子。

#### 渲染方程的级数解

$$
L = E + KL
$$

$$
(I - K)L = E
$$

$$
L = (I - K)^{-1}E
$$

利用二项式展开：

$$
L = (I + K + K^2 + K^3 + \cdots)E = E + KE + K^2E + K^3E + \cdots
$$

各项的物理意义：

| 项 | 物理意义 | 光栅化能否处理 |
|:---|:---|:---:|
| $E$ | 直接来自光源的光 | 能 |
| $KE$ | 直接光照（光弹射一次） | 能 |
| $K^2E$ | 间接光照（光弹射两次，如镜面反射） | 不能 |
| $K^3E$ | 两次间接光照 | 不能 |
| $K^nE$ | $n-1$ 次间接光照 | 不能 |

<!-- 截图：不同弹射次数的全局光照效果对比（L15 第37-43页）→ 保存为 ./images/chap7_18.png -->

光栅化本质上只能处理 $E + KE$（直接光照），无法处理更高次的间接光照。而光线追踪（路径追踪）可以自然地模拟所有次数的弹射。

---

### 12、概率论复习

#### 随机变量

随机变量 $X$ 服从概率密度函数 $p(x)$，记作 $X \sim p(x)$。

**离散情况**：
- $p_i \geq 0$
- $\sum_{i=1}^{n} p_i = 1$
- 期望值：$E[X] = \sum_{i=1}^{n} x_i p_i$

**骰子示例**（L15 第44-46页）：六面骰 $X$ 取值 1-6，各值概率 $p_i = 1/6$，期望：

$$
E[X] = \sum_{i=1}^{6} x_i p_i = \frac{1 + 2 + 3 + 4 + 5 + 6}{6} = 3.5
$$

**连续情况**：
- $p(x) \geq 0$
- $\int p(x) \, dx = 1$
- 期望值：$E[X] = \int x \, p(x) \, dx$

#### 随机变量函数的期望

若 $Y = f(X)$，则：

$$
E[Y] = E[f(X)] = \int f(x) \, p(x) \, dx
$$

---

### 13、蒙特卡洛积分 (Monte Carlo Integration)

#### 为什么需要蒙特卡洛积分

渲染方程涉及对半球面的积分，且被积函数（入射辐射率 $L_i$）通常是未知的、复杂的。解析求解不可行，需要数值方法。

#### 定义

给定定积分 $\int_a^b f(x) \, dx$，通过随机采样来估计：

$$
\int_a^b f(x) \, dx \approx F_N = \frac{1}{N} \sum_{i=1}^{N} \frac{f(X_i)}{p(X_i)}
$$

其中 $X_i \sim p(x)$ 是按概率密度函数 $p(x)$ 采样的随机变量。

#### 无偏估计

蒙特卡洛估计是**无偏的**：

$$
E[F_N] = \int_a^b f(x) \, dx
$$

即无论采样数 $N$ 多少，估计的期望值都等于真实积分值。

#### 均匀采样特例

当 $p(x) = \frac{1}{b-a}$（均匀分布）时：

$$
F_N = \frac{b-a}{N} \sum_{i=1}^{N} f(X_i)
$$

#### 重要性质

- 采样数 $N$ 越多，方差越小（估计越准确）
- 在 $x$ 上采样，就在 $x$ 上积分
- 可以使用任意 PDF $p(x)$，只要在 $f(x) \neq 0$ 的地方 $p(x) > 0$

---

### 14、路径追踪 (Path Tracing)

#### Whitted-Style 的问题

| 问题 | 说明 |
|:---|:---|
| **问题 1** | 光泽 (glossy) 材质不应做完美镜面反射 |
| **问题 2** | 漫反射表面之间应有间接光照（色彩渗透） |

渲染方程是正确的，但涉及半球面积分和递归执行。如何数值求解？蒙特卡洛积分！

#### 直接光照的蒙特卡洛解法

计算点 $p$ 沿 $\omega_o$ 方向的出射辐射率（仅直接光照）：

$$
L_o(p, \omega_o) = \int_{\Omega^+} L_i(p, \omega_i) \, f_r(p, \omega_i, \omega_o) \, (n \cdot \omega_i) \, d\omega_i
$$

使用蒙特卡洛积分，在半球面上均匀采样：

$$
L_o(p, \omega_o) \approx \frac{1}{N} \sum_{i=1}^{N} \frac{L_i(p, \omega_i) \, f_r(p, \omega_i, \omega_o) \, (n \cdot \omega_i)}{p(\omega_i)}
$$

其中 $p(\omega_i) = \frac{1}{2\pi}$（半球面均匀采样的 PDF）。

**伪代码**：

```
shade(p, wo):
    Lo = 0.0
    随机选择 N 个方向 wi ~ pdf
    for each wi:
        发射光线 r(p, wi)
        if 光线 r 击中光源:
            Lo += (1/N) * L_i * f_r * cos / pdf(wi)
    return Lo
```

#### 引入全局光照

如果光线击中的不是光源而是另一个物体 $q$，则 $q$ 也会反射光到 $p$。递归计算：

```
shade(p, wo):
    随机选择 N 个方向 wi ~ pdf
    Lo = 0.0
    for each wi:
        发射光线 r(p, wi)
        if 光线 r 击中光源:
            Lo += (1/N) * L_i * f_r * cos / pdf(wi)
        else if 光线 r 击中物体 q:
            Lo += (1/N) * shade(q, -wi) * f_r * cos / pdf(wi)
    return Lo
```

#### 问题 1：光线数量爆炸

每次弹射产生 $N$ 条光线，$k$ 次弹射后光线数量为 $N^k$，指数爆炸！

**解决方案**：令 $N = 1$，每次着色点只追踪一条光线。这就是**路径追踪 (Path Tracing)**（课件注：$N \neq 1$ 时称为分布式光线追踪 Distributed Ray Tracing，L16 第28页）。

```
shade(p, wo):
    随机选择 1 个方向 wi ~ pdf
    发射光线 r(p, wi)
    if 光线 r 击中光源:
        return L_i * f_r * cos / pdf(wi)
    else if 光线 r 击中物体 q:
        return shade(q, -wi) * f_r * cos / pdf(wi)
```

**噪声问题**：每像素只追踪一条路径会很嘈杂。解决方案：对每个像素发射多条路径，取平均。

```
ray_generation(camPos, pixel):
    在像素内均匀选择 N 个采样位置
    pixel_radiance = 0.0
    for each sample:
        发射光线 r(camPos, cam_to_sample)
        if 光线击中场景点 p:
            pixel_radiance += (1/N) * shade(p, sample_to_cam)
    return pixel_radiance
```

#### 问题 2：递归不终止

光在场景中无限弹射，递归永远不停止。截断弹射次数 = 丢失能量。

**解决方案：俄罗斯轮盘赌 (Russian Roulette, RR)**

<!-- 截图：Russian Roulette 概念（L16 第34页）→ 保存为 ./images/chap7_19.png -->

**思想**：
- 设定概率 $P$（$0 < P < 1$）
- 以概率 $P$：继续追踪光线，返回结果除以 $P$，即 $L_o / P$
- 以概率 $1 - P$：停止追踪，返回 0

**期望值不变**：

$$
E = P \cdot \frac{L_o}{P} + (1 - P) \cdot 0 = L_o
$$

**伪代码**：

```
shade(p, wo):
    设定概率 P_RR
    在 [0, 1] 均匀随机选择 ksi
    if (ksi > P_RR) return 0.0

    随机选择 1 个方向 wi ~ pdf
    发射光线 r(p, wi)
    if 光线 r 击中光源:
        return L_i * f_r * cos / pdf(wi) / P_RR
    else if 光线 r 击中物体 q:
        return shade(q, -wi) * f_r * cos / pdf(wi) / P_RR
```

#### 优化：直接光照采样

**问题**：均匀采样半球面时，大量光线被"浪费"（未击中光源）。课件示例（L16 第38页）：每 5 条光线中只有 1 条能命中光源。

**解决方案**：对光源进行直接采样。

蒙特卡洛允许任意采样方式。对光源面积均匀采样：$pdf = \frac{1}{A}$

但渲染方程在立体角上积分，需要转换到面积上的积分。利用微分立体角与面积的关系：

$$
d\omega = \frac{dA \cos\theta'}{\|x' - x\|^2}
$$

其中 $\theta'$ 是光源表面法线与连接方向的夹角。

代入渲染方程：

$$
L_o(x, \omega_o) = \int_A L_i(x, \omega_i) \, f_r(x, \omega_i, \omega_o) \, \frac{\cos\theta \cos\theta'}{\|x' - x\|^2} \, dA
$$

现在变成了对光源面积的积分！

**最终路径追踪算法**（分离直接光照和间接光照）：

```
shade(p, wo):
    # 直接光照：对光源采样（不需要 RR）
    L_direct = 0.0
    在光源上均匀采样一点 x'，pdf_light = 1/A
    发射光线 r(p, x')
    if 光线未被遮挡:
        L_direct = L_i * f_r * cos_theta * cos_theta' / |x'-x|^2 / pdf_light

    # 间接光照：对半球采样（需要 RR）
    L_indirect = 0.0
    在 [0,1] 均匀随机选择 ksi
    if (ksi <= P_RR):
        随机选择 1 个方向 wi ~ pdf
        发射光线 r(p, wi)
        if 光线击中物体 q:
            L_indirect = shade(q, -wi) * f_r * cos / pdf / P_RR

    return L_direct + L_indirect
```

#### 路径追踪总结

路径追踪是现代渲染的核心算法：

| 特性 | 说明 |
|:---|:---|
| **正确性** | 基于渲染方程，物理正确 |
| **全局光照** | 自然支持多次光线弹射 |
| **无偏** | 蒙特卡洛积分保证无偏 |
| **效率** | 通过 RR 和直接光照采样优化 |
| **质量** | SPP (samples per pixel) 越高，结果越准确 |

<!-- 截图：路径追踪不同 SPP 的效果对比（L16 第37页）→ 保存为 ./images/chap7_20.png -->

#### 路径追踪正确吗？

是的——几乎 100% 正确，即**照片级真实 (photo-realistic)**（L16 第46页）。Cornell box 的真实照片与路径追踪渲染的全局光照结果几乎无法区分。

<!-- 截图：Cornell box 真实照片与路径追踪全局光照对比（L16 第46页）→ 保存为 ./images/chap7_21.png -->

#### 光线追踪：旧概念与新概念（L16 第47页）

| 概念 | 定义 |
|:---|:---|
| **旧** | 光线追踪 = Whitted-Style 光线追踪 |
| **新** | 光线传输的通用解，包括（单向/双向）路径追踪、光子映射、MLT、VCM/UPBP 等 |

#### 课程未覆盖的主题（L16 第48-49页）

- 如何均匀采样半球？如何推广到采样任意函数？(sampling)
- 蒙特卡洛允许任意 PDF，最优选择是什么？(importance sampling 重要性采样)
- 随机数的质量重要吗？(low discrepancy sequences 低差异序列)
- 同时采样半球和光源，可以结合吗？(multiple importance sampling 多重重要性采样)
- 像素辐射率为何是所有穿过它的路径辐射率的平均？(pixel reconstruction filter 像素重建滤波器)
- 像素的辐射率就是像素的颜色吗？——不是（gamma 校正、曲线、色彩空间）

---

### 15、材质与外观 (Materials and Appearances)

在图形学中，**材质 = BRDF**。不同的 BRDF 定义了不同的材质外观。

#### 漫反射材质 (Diffuse / Lambertian Material)

光线被均匀地反射到所有方向。

**BRDF**：

$$
f_r = \frac{\rho}{\pi}
$$

其中 $\rho$ 是反照率 (albedo)，即颜色。

**推导**：假设入射光照均匀（$L_i$ 为常数），由能量守恒：

$$
L_o(\omega_o) = \int_{H^2} f_r \, L_i \cos\theta_i \, d\omega_i = f_r \, L_i \int_{H^2} \cos\theta_i \, d\omega_i = \pi f_r L_i
$$

由于半球面上 $\int_{H^2} \cos\theta_i \, d\omega_i = \pi$，若 $L_o = L_i$（完全反射），则 $f_r = \frac{1}{\pi}$。

#### 光泽材质 (Glossy Material)

介于漫反射和完美镜面之间，如铜、铝等金属表面。反射光集中在镜面方向附近的一个小范围内。

<!-- 截图：光泽材质渲染效果（铜、铝，L17 第10页）→ 保存为 ./images/chap7_22.png -->

#### 理想反射/折射材质 (Ideal Reflective / Refractive Material)

也称为 BSDF（Bidirectional Scattering Distribution Function），同时处理反射和折射。

##### 完美镜面反射 (Perfect Specular Reflection)

入射角 = 反射角：$\theta_i = \theta_o = \theta$

方位角关系：$\phi_o = (\phi_i + \pi) \mod 2\pi$

反射方向计算：

$$
\omega_o = -\omega_i + 2(\omega_i \cdot \vec{n})\vec{n}
$$

<!-- 截图：镜面反射方向计算（俯视图，L17 第14页）→ 保存为 ./images/chap7_23.png -->

完美镜面反射的 BRDF 可以用 Dirac $\delta$ 函数形式表示（L17 第15页）。

##### 镜面折射 (Specular Refraction)

**斯涅尔定律 (Snell's Law)**：

$$
\eta_i \sin\theta_i = \eta_t \sin\theta_t
$$

其中 $\eta_i$ 和 $\eta_t$ 分别是入射介质和折射介质的折射率 (IOR)。

**常见折射率**：

| 介质 | 折射率 |
|:---|:---|
| 真空 | 1.0 |
| 空气（海平面） | 1.00029 |
| 水 (20C) | 1.333 |
| 玻璃 | 1.5 - 1.6 |
| 钻石 | 2.42 |

**折射角计算**：

$$
\cos\theta_t = \sqrt{1 - \left(\frac{\eta_i}{\eta_t}\right)^2 \sin^2\theta_i} = \sqrt{1 - \left(\frac{\eta_i}{\eta_t}\right)^2 (1 - \cos^2\theta_i)}
$$

**全内反射 (Total Internal Reflection)**：当光从光密介质射向光疏介质（$\eta_i / \eta_t > 1$）且入射角足够大时：

$$
1 - \left(\frac{\eta_i}{\eta_t}\right)^2 (1 - \cos^2\theta_i) < 0
$$

此时光无法折射出去，全部反射回介质内。

**斯涅尔窗 (Snell's Window / Circle)**（L17 第19页）：全内反射的直观现象——在水下仰望水面时，只能透过头顶一个圆形区域看到水面外的世界；圆形边界之外因全内反射呈现水底的镜像。

<!-- 截图：菲涅尔项曲线：电介质与导体（L17 第21-22页）→ 保存为 ./images/chap7_24.png -->

##### 菲涅尔项 (Fresnel Term)

**现象**：反射率取决于入射角（掠射角时反射率增大）。

| 类型 | 说明 |
|:---|:---|
| **电介质 (Dielectric)** | 如玻璃、水，反射率在掠射角显著增大 |
| **导体 (Conductor)** | 如金属，反射率在所有角度都较高 |

**精确公式**（考虑偏振）：

$$
R_s = \left|\frac{n_1 \cos\theta_i - n_2 \cos\theta_t}{n_1 \cos\theta_i + n_2 \cos\theta_t}\right|^2
$$

$$
R_p = \left|\frac{n_1 \cos\theta_t - n_2 \cos\theta_i}{n_1 \cos\theta_t + n_2 \cos\theta_i}\right|^2
$$

$$
R_{eff} = \frac{1}{2}(R_s + R_p)
$$

**Schlick 近似**（更高效的近似）：

$$
R(\theta) = R_0 + (1 - R_0)(1 - \cos\theta)^5
$$

其中：

$$
R_0 = \left(\frac{n_1 - n_2}{n_1 + n_2}\right)^2
$$

#### 微表面材质 (Microfacet Material)

##### 微表面理论

- **宏观尺度**：表面平坦且粗糙
- **微观尺度**：表面凹凸不平，每个微表面像一面小镜子（微面元，microfacet）

<!-- 截图：微表面理论示意（L17 第26页）→ 保存为 ./images/chap7_25.png -->

##### 微表面 BRDF

关键：微面元法线的分布。

- 法线集中 $\Rightarrow$ 光泽 (glossy)
- 法线分散 $\Rightarrow$ 漫反射 (diffuse)

$$
f(i, o) = \frac{F(i, h) \, G(i, o, h) \, D(h)}{4(n, i)(n, o)}
$$

| 项 | 含义 |
|:---|:---|
| $F(i, h)$ | **菲涅尔项**：多少光被反射 |
| $G(i, o, h)$ | **阴影-遮蔽项**：微面元间的遮挡 |
| $D(h)$ | **法线分布函数 (NDF)**：微面元法线的分布 |
| $h$ | **半向量**：$\omega_i$ 和 $\omega_o$ 的中间方向 |

#### 各向同性与各向异性材质

| 类型 | 说明 | BRDF 性质 | 示例 |
|:---|:---|:---|:---|
| **各向同性** | 微表面法线无方向性偏好 | $f_r(\theta_i, \phi_i; \theta_r, \phi_r) = f_r(\theta_i, \theta_r, |\phi_r - \phi_i|)$ | 大部分材质 |
| **各向异性** | 微表面有方向性结构 | $f_r(\theta_i, \phi_i; \theta_r, \phi_r) \neq f_r(\theta_i, \theta_r, \phi_r - \phi_i)$ | 拉丝金属、尼龙、天鹅绒 |

<!-- 截图：各向同性与各向异性材质对比（L17 第31页）→ 保存为 ./images/chap7_26.png -->

各向异性材质的反射特性随方位角变化，来源于表面的方向性微结构（如拉丝金属的纹路方向）。

#### BRDF 的性质

| 性质 | 公式 | 说明 |
|:---|:---|:---|
| **非负性** | $f_r(\omega_i \to \omega_r) \geq 0$ | 反射光不能为负 |
| **线性** | $L_r = \int f_r L_i \cos\theta_i \, d\omega_i$ | 多个 BRDF 可以线性叠加 |
| **可逆性** | $f_r(\omega_r \to \omega_i) = f_r(\omega_i \to \omega_r)$ | 交换入射和出射方向，BRDF 不变 |
| **能量守恒** | $\forall \omega_r, \int_{H^2} f_r(\omega_i \to \omega_r) \cos\theta_i \, d\omega_i \leq 1$ | 反射能量不超过入射能量 |

#### BRDF 测量

**动机**：避免开发和推导模型，直接用真实材质数据渲染。

**方法**（使用 gonioreflectometer）：

```
for each 出射方向 wo:
    将光源移动到 wo 方向照射表面
    for each 入射方向 wi:
        将传感器移动到 wi 方向
        测量入射辐射率
```

**效率优化**（L17 第44页）：
- 各向同性表面将维度从 4D 降到 3D
- 可逆性将测量数减半
- 更巧妙的光学系统设计

**基于图像的测量**（L17 第42页）：Marschner et al. 1999 提出用相机拍摄的方式代替逐点机械测量。

**测量的挑战**（L17 第45页）：
- 掠射角处的精确测量（因菲涅尔效应而重要）
- 高频镜面需要足够密集的采样才能捕捉
- 后向反射 (retro-reflection)
- 空间变化的反射率

**测量数据的表示**（L17 第46-47页）：期望性质——紧凑、准确还原测量数据、任意方向对可高效求值、利于重要性采样。常用**表格表示**：在 $(\theta_i, \theta_o, |\phi_i - \phi_o|)$ 上存储规则采样的值（必要时重参数化以更好匹配镜面峰）。存储需求很高，例如 **MERL BRDF 数据库** [Matusik et al. 2004] 包含 90 × 90 × 180 组测量。

---

### 16、高级渲染主题 (Advanced Topics in Rendering)

#### 有偏与无偏

| 类型 | 定义 | 特点 |
|:---|:---|:---|
| **无偏 (Unbiased)** | 估计的期望值等于真实值，无论采样数多少 | 无系统误差 |
| **有偏 (Biased)** | 估计的期望值不等于真实值 | 有系统误差 |
| **一致 (Consistent)** | 有偏但采样数趋于无穷时收敛到正确值 | 有偏但可用 |

有偏的一个直观理解：**有偏 = 模糊**，**一致 = 无穷采样时不模糊**。

#### 无偏光线传输方法

##### 双向路径追踪 (BDPT)

**思想**：
- 从相机和光源分别追踪子路径
- 连接两条子路径的端点

<!-- 截图：双向路径追踪示意（L18 第6页）→ 保存为 ./images/chap7_27.png -->

**适用场景**：光在光源侧的传输复杂时效果好。

**缺点**：实现复杂，速度较慢。

##### Metropolis 光线传输 (MLT)

**思想**：基于马尔可夫链蒙特卡洛 (MCMC)，从当前样本跳转到下一个样本。

**特点**：
- 非常擅长局部探索困难的光路
- 通过扰动现有路径生成新路径

**优点**：对困难光路效果好，无偏。

**缺点**：
- 难以估计收敛速度
- 各像素收敛速度不均，结果"脏"
- 不适合渲染动画

#### 有偏光线传输方法

##### 光子映射 (Photon Mapping)

**两阶段方法**，非常擅长处理 SDS（镜面-漫反射-镜面）路径和生成焦散效果。

<!-- 截图：光子映射两阶段：光子追踪与光子收集（L18 第12页）→ 保存为 ./images/chap7_28.png -->

**阶段 1：光子追踪**
- 从光源发射光子，在场景中弹射
- 在漫反射表面记录光子

**阶段 2：光子收集 (Final Gathering)**
- 从相机发射子路径，弹射到漫反射表面
- 基于局部光子密度估计光照

**密度估计**：光子密度高的区域更亮。对每个着色点，找最近的 $N$ 个光子，计算覆盖面积 $\Delta A$：

$$
\text{密度} \approx \frac{\Delta N}{\Delta A}
$$

**为什么是有偏的**：$dN/dA \neq \Delta N / \Delta A$，但发射更多光子时 $\Delta A \to dA$，所以是一致的。

**课件设问**（L18 第15页）：为什么不用"固定范围搜索"做密度估计？——固定范围无法自适应局部光子密度，在光子稀疏处会引入偏差（答案来自课程视频，课件外补充）。

| $N$ 小 | $N$ 大 |
|:---|:---|
| 噪声大 | 模糊 |

##### 顶点连接与合并 (VCM)

BDPT 和光子映射的组合：
- BDPT 中端点无法连接但可以合并时，使用光子映射处理

#### 即时辐射度 (Instant Radiosity)

也称为多光源方法 (Many-Light Methods)。

**思想**：被照亮的表面可以当作光源。

**方法**：
1. 从光源发射子路径，将每个子路径的端点作为虚拟点光源 (VPL)
2. 使用这些 VPL 照常渲染场景

**优点**：快速，漫反射场景效果好。

**缺点**：VPL 接近着色点时出现尖刺，无法处理光泽材质。

#### 高级外观建模

##### 非表面模型

| 模型 | 说明 | 示例 |
|:---|:---|:---|
| **参与介质 (Participating Media)** | 光在介质中被吸收和散射，使用相位函数描述散射 | 雾、云 |
| **毛发/毛皮 (Hair/Fur)** | BCSDF，玻璃柱体模型 (Marschner)，双层圆柱模型 (Double Cylinder) | 人物头发、动物毛皮 |
| **颗粒材质 (Granular Material)** | 程序化定义颗粒，避免显式建模所有颗粒 | 沙子、糖 |

##### 参与介质的渲染（L18 第24-26页）

光在参与介质中传播时，任意一点都可能被（部分）**吸收**和**散射**；用**相位函数 (phase function)** 描述任意一点处光散射的角度分布。

渲染方法（三步）：
1. 随机选择一个弹射方向
2. 随机选择一段直行距离
3. 在每个"着色点"处连接光源

应用：《超能陆战队》(Big Hero 6, 2014) 的雾效、《刺客信条：枭雄》(Assassin's Creed Syndicate, 2015) 的大气效果。

<!-- 截图：参与介质：吸收、散射与相位函数（L18 第24-25页）→ 保存为 ./images/chap7_29.png -->

##### 毛发外观模型（L18 第31-35页）

| 模型 | 思想 |
|:---|:---|
| **Kajiya-Kay 模型** | 把头发当作无光泽圆柱，基于切线的经验反射模型 |
| **Marschner 模型** | 把头发当作玻璃圆柱，精确考虑光在圆柱内的传播 |

Marschner 模型中光与头发的三种交互：**R**（反射）、**TT**（透射）、**TRT**（内壁反射后再透射）。

<!-- 截图：Marschner 毛发模型：玻璃圆柱与 R/TT/TRT 三种光交互（L18 第34页）→ 保存为 ./images/chap7_30.png -->

##### 毛发 vs 毛皮（L18 第38-47页）

动物毛皮与人类头发的关键区别：毛皮纤维中心有**髓质 (medulla)**——结构复杂、会散射光；髓质越大，外观越散射、越不饱和。用人类头发模型渲染毛皮无法表现这种漫反射式的饱和外观。

**双层圆柱模型 (Double Cylinder Model)** [Yan et al. 2015, 2017]：在 Marschner 单层（表皮 cuticle + 皮层 cortex）圆柱内部增加髓质层，产生更多光路瓣 (lobes)：R、TT、TRT、TTs、TRTs（s 表示经过髓质散射）。

应用：《猩球崛起 3》(2017)、《狮子王》(2019) 均获奥斯卡最佳视觉效果提名。

<!-- 截图：毛发 vs 毛皮：髓质结构与双层圆柱模型的 lobes（L18 第42-43页）→ 保存为 ./images/chap7_31.png -->

##### 表面模型

| 模型 | 说明 | 示例 |
|:---|:---|:---|
| **半透明材质 (BSSRDF)** | 次表面散射，光从一点进入、另一点射出 | 玉石、水母、皮肤 |
| **布料 (Cloth)** | 编织/针织纤维，可作为表面/参与介质/实际纤维渲染 | 衣物 |
| **细节材质 (Detailed Material)** | 非统计 BRDF，使用法线贴图定义微表面细节 | 金属闪粉、刮痕 |

##### 次表面散射 (Subsurface Scattering)

光从表面一点进入，在材质内部散射后从另一点射出。违反了 BRDF 的基本假设（入射点和出射点相同）。

**BSSRDF**：BRDF 的推广：

$$
S(x_i, \omega_i, x_o, \omega_o)
$$

对应的渲染方程推广为对表面所有点和所有方向的积分：

$$
L(x_o, \omega_o) = \int_A \int_{H^2} S(x_i, \omega_i, x_o, \omega_o) \, L_i(x_i, \omega_i) \cos\theta_i \, d\omega_i \, dA
$$

**偶极子近似 (Dipole Approximation)** [Jensen et al. 2001]（L18 第57页）：不直接求解 BSSRDF 积分，而是在半无限介质表面下方引入一正一负两个**虚拟点光源**，用它们近似光在介质内部的扩散传输。

<!-- 截图：偶极子近似：真实光源与虚拟光源对（L18 第57页）→ 保存为 ./images/chap7_32.png -->

##### 细节材质 / 闪光材质 (Detailed / Glinty Material)（L18 第70-88页）

**动机**：统计微表面模型的 NDF 是平滑分布，渲染结果"太干净"，不像真实世界——真实车漆有闪粉、真实金属有划痕和污渍。

| 对比 | 统计微表面 | 真实表面 |
|:---|:---|:---|
| NDF | 平滑的统计分布 | 实际法线分布有尖锐细节 |
| 法线贴图分辨率 | 通常几千 | 约 200K × 200K |

**难点**：直接对高频法线贴图做路径采样极其困难——课件示例中朴素采样 2 小时的结果与需要 21.3 天以上才能收敛的目标相差甚远。

**解决方案：P-NDF（BRDF over a pixel）**：不再逐法线采样，而是考虑一个像素足迹覆盖的法线贴图小块 (patch) 的整体 NDF。P-NDF 具有尖锐特征，不同法线贴图产生不同形状（Blender 形状、拉丝金属、椭球凸起、海浪等）。

应用：《古墓丽影：崛起》(Rise of the Tomb Raider, 2016) 的闪光材质。

<!-- 截图：统计 NDF vs 实际 NDF（L18 第76页）→ 保存为 ./images/chap7_33.png -->
<!-- 截图：P-NDF 的形状（L18 第83-87页）→ 保存为 ./images/chap7_34.png -->

##### 波动光学外观 (Wave Optics)（L18 第89-96页）

几何光学的 BRDF 无法解释**衍射**现象：CD、金属膜、手机屏幕的彩虹色，以及划痕金属、MacBook 铝壳上的各向异性彩色反射。将表面视为高度场 (heightfield)、按波动光学计算 BRDF，可以重现这些效果 [Yan et al. 2018]。

<!-- 截图：波动光学细节材质：MacBook 铝壳渲染（L18 第92-93页）→ 保存为 ./images/chap7_35.png -->

##### 程序化外观 (Procedural Appearance)（L18 第98-104页）

不需要纹理，通过噪声函数实时计算细节：

- **3D 噪声**：物体被切开或打破时，内部结构也随之存在
- **阈值化 (thresholding)**：把连续噪声二值化，例如

```python
if noise(x, y, z) > threshold:
    reflectance = 1
else:
    reflectance = 0
```

- 复杂噪声函数功能非常强大（如 Worley 噪声等）

<!-- 截图：程序化外观：3D 噪声内部结构与阈值化（L18 第98-100页）→ 保存为 ./images/chap7_36.png -->

---
