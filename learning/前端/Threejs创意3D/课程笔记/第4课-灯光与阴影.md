# 第 4 课：灯光与阴影

> 日期：2026-07-17
> 状态：已完成
> 评分：9.5/10

---

## 主题目标

- 掌握 6 种灯光类型及适用场景
- 理解阴影的三要素（光源、投射物、接收面）
- 学会调整阴影质量和性能的平衡

---

## 精选资源

| 资源 | 说明 | 用法 |
|------|------|------|
| [Three.js Lights 文档](https://threejs.org/docs/#api/en/lights/AmbientLight) | API 参考 | 查参数时用 |
| [Three.js Journey — Lights](https://threejs-journey.com/lessons/lights) | 灯光章节 | 边看边做 |

### 课程物料

| 类别 | 物料 | 来源 | 说明 |
|------|------|------|------|
| 纹理 | concrete-floor.jpg | https://ambientcg.com | 地面纹理，用于接收阴影（外部加载） |
| 模型 | suzanne.glb | 自制或下载 | 猴头模型，用于阴影测试（外部加载） |
| HDRI | outdoor.hdr | https://polyhaven.com/hdris | 室外环境光参考（外部加载） |

**代码实际使用的物料**：
- 地面纹理：通过 `TextureLoader` 加载 concrete-floor.jpg（外部加载，待下载）
- 猴头模型：使用 `GLTFLoader` 加载 `suzanne.glb`（用户上传，放置在 `public/models/`）
- 灯光：程序创建 6 种灯光类型（Ambient/Hemisphere/Directional/Point/Spot/RectArea）
- 几何体：程序创建球体、立方体、圆环（内置几何体）

---

## 核心知识点

### 1. 灯光类型对比

| 灯光 | 作用 | 适用场景 | 性能 |
|------|------|----------|------|
| AmbientLight | 全局均匀照明，无方向 | 补充环境光，减少阴影 | 最快 |
| HemisphereLight | 半球照明（天空+地面） | 室外场景，自然过渡 | 快 |
| DirectionalLight | 平行光，模拟太阳 | 室外场景，平行阴影 | 中 |
| PointLight | 点光源，向四周发光 | 台灯、蜡烛、灯泡 | 中 |
| SpotLight | 聚光灯，锥形光束 | 手电筒、舞台灯 | 慢 |
| RectAreaLight | 矩形面光源，一整面发光 | 橱窗、灯箱、屏幕补光 | 慢（不支持阴影） |

六种灯光各配一句生活类比，先有画面再记参数：

- AmbientLight 环境光：像阴天的散射光，没有方向，所有面被均匀照亮。
- HemisphereLight 半球光：像天空加地面反光——上半染天空色、下半染地面色，室外一盏就有自然过渡。
- DirectionalLight 平行光：像太阳，光线平行射来，只有方向没有位置感。
- PointLight 点光源：像裸灯泡，从一个点向四面八方发光，越远越暗。
- SpotLight 聚光灯：像手电筒，锥形光束，有明确的目标和照射范围。
- RectAreaLight 面光源：像灯箱，一整面都在发光，高光呈矩形。

**代码示例**：
```typescript
// 环境光
const ambientLight = new THREE.AmbientLight(0xffffff, 0.4)

// 半球光
const hemisphereLight = new THREE.HemisphereLight(0x87ceeb, 0x362907, 0.6)

// 方向光（模拟太阳）
const directionalLight = new THREE.DirectionalLight(0xffffff, 1.0)
directionalLight.position.set(5, 8, 5)

// 点光源
const pointLight = new THREE.PointLight(0xff922b, 2, 20)
pointLight.position.set(-3, 4, 2)

// 聚光灯
const spotLight = new THREE.SpotLight(0xffffff, 1, 20, Math.PI / 6)
spotLight.position.set(0, 8, 0)

// 矩形面光源（一整面墙都是光源）
const rectAreaLight = new THREE.RectAreaLight(0x00ffff, 5, 4, 3)
rectAreaLight.position.set(5, 3, 5)
rectAreaLight.lookAt(0, 1, 0)
```

### 2. 阴影三要素

开启阴影需要四步设置：

```typescript
// 1. 光源：设置 castShadow = true
directionalLight.castShadow = true

// 2. 投射物：设置 castShadow = true
mesh.castShadow = true

// 3. 接收面：设置 receiveShadow = true
ground.receiveShadow = true

// 4. 渲染器：开启阴影
renderer.shadowMap.enabled = true
renderer.shadowMap.type = THREE.PCFSoftShadowMap
```

### 3. 阴影质量与性能

**shadow.mapSize**：阴影贴图分辨率
- 512×512：低质量，性能最好
- 1024×1024：中等质量，推荐
- 2048×2048：高质量，性能较差
- 4096×4096：极高质量，性能最差

**shadow.camera**：阴影相机范围
- 范围太大 → 阴影模糊（像素被拉伸）
- 范围太小 → 阴影被裁切

```typescript
directionalLight.shadow.mapSize.width = 1024
directionalLight.shadow.mapSize.height = 1024
directionalLight.shadow.camera.near = 0.5
directionalLight.shadow.camera.far = 50
directionalLight.shadow.camera.left = -10
directionalLight.shadow.camera.right = 10
directionalLight.shadow.camera.top = 10
directionalLight.shadow.camera.bottom = -10
```

### 4. 灯光 Helper

Three.js 提供了灯光 Helper，用于可视化灯光范围和方向：

```typescript
// 方向光 Helper
const directionalHelper = new THREE.DirectionalLightHelper(directionalLight, 1)
scene.add(directionalHelper)

// 聚光灯 Helper
const spotHelper = new THREE.SpotLightHelper(spotLight)
scene.add(spotHelper)

// 点光源 Helper
const pointHelper = new THREE.PointLightHelper(pointLight, 0.5)
scene.add(pointHelper)

// 矩形面光源 Helper
const rectAreaHelper = new THREE.RectAreaLightHelper(rectAreaLight)
scene.add(rectAreaHelper)
```

### 5. RectAreaLight 矩形面光源

从一个矩形平面均匀发光，能实现「一整面墙都是光源」的效果，
适合橱窗、灯箱、广告牌、屏幕补光等场景。

```typescript
import { RectAreaLightUniformsLib } from 'three/addons/lights/RectAreaLightUniformsLib.js'
import { RectAreaLightHelper } from 'three/addons/helpers/RectAreaLightHelper.js'

// 必须先初始化 uniform 库，否则光照结果不正确（整个程序调用一次即可）
RectAreaLightUniformsLib.init()

const rectAreaLight = new THREE.RectAreaLight(0x00ffff, 5, 4, 3)
rectAreaLight.position.set(5, 3, 5)
rectAreaLight.lookAt(0, 1, 0) // 沿自身 -Z 方向照射
scene.add(rectAreaLight)
```

理解要点：
- **必须先 `RectAreaLightUniformsLib.init()`**：内部预计算矩形面的 LTC 纹理，跳过这一步光照结果不正确
- **不支持投影**：设置 `castShadow` 无效，这是它与 SpotLight 的关键区别
- **性能开销大**：谨慎在低端设备上大量使用

---

## 代码场景结构

代码中创建了一个综合展示场景，包含 3 个部分：

### 第 1 部分：6 种灯光对比

5 个相同几何体，分别使用不同灯光照射，对比效果。

### 第 2 部分：阴影展示

- 方向光阴影：模拟太阳光，平行阴影
- 点光源阴影：向四周投射阴影
- 聚光灯阴影：锥形光束阴影

### 第 3 部分：阴影质量对比

- 低质量（512×512）
- 中质量（1024×1024）
- 高质量（2048×2048）

---

## 动手练习（课后作业）

- [x] 运行项目，观察 5 种灯光的效果差异
- [x] 观察阴影的三要素设置
- [x] 调整 shadow.mapSize，观察阴影质量变化
- [x] 使用灯光 Helper 可视化灯光范围
- [x] 调整 shadow.camera 参数，观察阴影范围变化
- [x] 使用下拉菜单切换不同灯光，对比效果
- [x] 观察单个灯光 vs 全部灯光的效果差异

---

## 常见错误

1. **阴影不显示**：忘记设置 `castShadow` 或 `receiveShadow`
2. **阴影模糊**：shadow.mapSize 太小
3. **阴影被裁切**：shadow.camera 范围太小
4. **性能卡顿**：shadow.mapSize 太大，或阴影灯光太多
5. **阴影有锯齿**：需要开启 PCFSoftShadowMap
6. **RectAreaLight 光照不对**：忘记先调用 `RectAreaLightUniformsLib.init()`；另外它不支持投影，`castShadow` 设置了也无效

---

## 复盘自测

1. **环境光和方向光的区别是什么？**
   > 环境光（AmbientLight）全局均匀照明、无方向，用来补充环境亮度、减少死黑阴影；方向光（DirectionalLight）是平行光，模拟太阳，有明确方向，能投出平行阴影。
   > 性能上环境光最快，方向光为中等。

2. **开启阴影需要哪三步设置？**
   > 笔记按四步记：光源 castShadow = true、投射物 mesh castShadow = true、接收面 ground receiveShadow = true，再由渲染器开启 renderer.shadowMap.enabled。
   > 按「三要素」归纳就是前三步（光源、投射物、接收面），渲染器开关是全局前提，漏了整个场景都没有阴影。

3. **shadow.mapSize 设太大/太小分别有什么问题？**
   > 太大：阴影贴图分辨率过高导致性能差（2048 性能较差，4096 最差，容易卡顿）。
   > 太小：阴影模糊（512 就是低质量）。推荐 1024×1024 中等质量。

4. **为什么聚光灯比点光源性能差？**
   > 灯光类型对比表中 SpotLight 性能为「慢」，PointLight 为「中」。聚光灯是锥形光束（比点光源多一个 angle 锥角参数），课程阴影展示里还用它投射锥形光束阴影，照明与阴影的综合开销比点光源大。

5. **灯光 Helper 有什么用途？**
   > 可视化灯光的范围和方向。DirectionalLightHelper、SpotLightHelper、PointLightHelper、RectAreaLightHelper 分别对应四种灯光，调灯光时用来确认位置、朝向和照射范围。

6. **RectAreaLight 使用前必须做什么？它和 SpotLight 的关键区别是什么？**
   > 必须先调用一次 RectAreaLightUniformsLib.init() 初始化 uniform 库，否则光照结果不正确。
   > 关键区别：RectAreaLight 不支持投影，设置 castShadow 无效；SpotLight 支持阴影。

---

## API 速查

| API | 用途 |
|-----|------|
| `new THREE.AmbientLight(color, intensity)` | 环境光 |
| `new THREE.HemisphereLight(skyColor, groundColor, intensity)` | 半球光 |
| `new THREE.DirectionalLight(color, intensity)` | 方向光 |
| `new THREE.PointLight(color, intensity, distance)` | 点光源 |
| `new THREE.SpotLight(color, intensity, distance, angle)` | 聚光灯 |
| `new THREE.RectAreaLight(color, intensity, width, height)` | 矩形面光源 |
| `RectAreaLightUniformsLib.init()` | 面光源使用前必须初始化（一次性） |
| `rectAreaLight.lookAt(x, y, z)` | 面光源朝向（沿 -Z 方向照射） |
| `light.castShadow = true` | 光源投射阴影 |
| `mesh.castShadow = true` | 物体投射阴影 |
| `mesh.receiveShadow = true` | 物体接收阴影 |
| `renderer.shadowMap.enabled = true` | 渲染器开启阴影 |
| `new THREE.DirectionalLightHelper(light, size)` | 方向光 Helper |
| `new THREE.SpotLightHelper(light)` | 聚光灯 Helper |
| `new THREE.PointLightHelper(light, size)` | 点光源 Helper |
| `new THREE.RectAreaLightHelper(light)` | 面光源 Helper |
